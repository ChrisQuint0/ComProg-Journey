#include <bits/stdc++.h>
using namespace std;

int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int a, b;
  cout << "Enter two numbers: ";
  cin >> a >> b;
  cout << "Sum is: " << a + b << "\n";
  return 0;
}